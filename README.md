# TypeScript Algorithm Production Template

A structured, strict-mode environment for implementing and testing algorithms using TypeScript and Jest.

## Structure
* `src/` - Production TypeScript files (compiles into `dist/`).
* `ts-test/` - Jest automation tests.